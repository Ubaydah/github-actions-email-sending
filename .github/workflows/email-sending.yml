name: email_sending

on:
  schedule:
    - cron: */3 * * * *
    
  workflow_dispatch:

jobs:
  email-job:
    runs-on: ubuntu-latest
    steps:
      - name: first-step
        run: echo "hello world"
